name: transaction_plus
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^4.0.0
  shared_preferences: ^2.0.5
  get: ^4.1.4
  call: ^0.3.0
  bot_toast: 4.0.0
  common_utils: ^1.1.3
  mqtt_client: ^9.3.1
  event_bus: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/data/
    - assets/dll/
